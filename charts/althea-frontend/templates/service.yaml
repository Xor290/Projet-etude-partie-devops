apiVersion: v1
kind: Service
metadata:
  name: {{ include "althea-frontend.fullname" . }}
  labels:
    {{- include "althea-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "althea-frontend.selectorLabels" . | nindent 4 }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
      protocol: TCP
